<div class="header">
    <div class="container">
        <div class="row">
            <div class="col-md-4 logo">
                <a href="{{ path('homepage') }}">SnowTricks</a>
            </div>
            <div class="col-md-8 menu">
                <a href="{{ path('homepage') }}"><i class="fas fa-home"></i></a>
                <a href="#">Figures</a>
                {% if app.user != null %}
                    <a href="{{ path('logout') }}"><i class="fa fa-sign-out"></i></a>
                {% else %}
                    <a href="{{ path('login') }}"><i class="fas fa-user"></i></a>
                {% endif %}
            </div>
        </div>
    </div>
</div>
<div class="separ-header-main"></div>


